# Chrome AI网页阅读助手插件产品需求文档（PRD）

## 1\. 文档概述

### 1\.1 文档目的

本文档为**Chrome AI网页阅读助手插件**的正式产品需求文档，明确插件核心功能、交互逻辑、技术规格、配置规则、适配场景，为前端开发、插件打包、测试验收提供唯一落地依据。同时基于用户核心诉求补充轻量化、高实用性的拓展功能，提升技术文档阅读、AI辅助问答的使用体验。

### 1\.2 产品定位

一款轻量化谷歌浏览器插件，主打**网页智能解析\+AI对话辅助**，聚焦技术文档、开源文档、技术博客、教程类网页场景，通过可配置大模型Agent，帮助用户快速读懂复杂网页内容、答疑解惑、深度讨论知识点，支持一键开关、右侧悬浮弹窗交互，全程不干扰原生网页浏览。

### 1\.3 核心用户

- 开发工程师、技术学习者：阅读英文技术文档、源码文档、框架手册、API文档时需要解读、答疑、举例说明；

- 科研/办公人群：需要快速总结长网页、解析专业内容、针对页面内容进行AI问答；

- 所有需要辅助阅读网页、针对性AI对话的浏览器用户。

### 1\.4 核心诉求落地总览

1\. AI Agent赋能网页：智能解析当前网页内容，重点优化技术文档阅读辅助；2\. 全自定义大模型配置：支持多模型、自定义接口、参数调节；3\. 全局开关控制：一键激活/关闭插件所有功能；4\. 右侧悬浮对话小窗：实时基于当前网页内容和AI Agent对话、答疑、讨论。

## 2\. 产品整体功能架构

插件整体分为五大核心模块，覆盖用户诉求\+优化体验的补充功能：插件开关模块、大模型配置模块、网页智能解析Agent模块、右侧悬浮对话窗模块、本地数据缓存模块。

1. 插件开关模块：控制插件全局启停，关闭后所有功能静默失效，无页面占用、无资源消耗；

2. 大模型配置模块：支持自定义模型接口、密钥、参数、模型类型，适配主流开源/商用大模型；

3. 网页AI Agent核心模块：自动抓取当前网页文本、过滤冗余内容、智能理解页面逻辑，针对性适配技术文档解析；

4. 右侧悬浮对话窗模块：可展开/收起、可拖拽、自适应尺寸，实现基于当前网页的专属AI对话；

5. 本地缓存模块：缓存对话记录、模型配置，无需重复设置，提升使用效率。

## 3\. 详细功能需求（核心\+补充优化功能）

### 3\.1 全局可激活开关功能（用户核心诉求3）

#### 3\.1\.1 功能描述

插件提供全局总开关，控制插件所有功能的启用与关闭，状态全局持久化，重启浏览器不重置。

#### 3\.1\.2 交互规则

- 开关入口：插件点击弹出的主面板顶部，设置醒目切换按钮（开启/关闭两种状态）；

- 开启状态：插件生效，自动监听当前网页，支持唤起右侧对话窗、AI解析、问答功能；

- 关闭状态：**完全静默**，不抓取网页内容、不占用内存、不弹窗、无后台运行，所有AI功能失效；

- 默认状态：插件首次安装默认开启。

#### 3\.1\.3 补充优化功能

支持**网页单独禁用**：针对特定网页（如隐私页面、简单页面），可单独关闭该页面的插件功能，不影响其他网页状态。

### 3\.2 可配置大模型系统（用户核心诉求2）

#### 3\.2\.1 功能描述

支持用户自主配置任意兼容OpenAI接口协议的大模型，实现模型自由切换、参数自定义，适配本地大模型、商用API、开源模型接口。

#### 3\.2\.2 配置项明细

|配置项|功能说明|是否必填|
|---|---|---|
|模型接口地址|支持自定义API域名，适配本地部署模型、第三方接口、官方接口|是|
|API密钥|用户个人密钥，本地加密存储，不上传云端|否（本地免密模型可空）|
|模型名称|自定义输入模型名，如gpt\-4o、deepseek、qwen、llama3等|是|
|温度值（Temperature）|0\-1区间，控制回答随机性，技术文档阅读默认0\.2（严谨模式）|可选，带默认值|
|最大上下文长度|限制单次对话token，避免超长页面请求失败|可选，带默认值|
|请求超时时间|自定义接口超时，适配本地低速模型|可选，带默认值|

#### 3\.2\.3 补充优化功能

- 模型配置预设：内置常用模型模板（通义千问、DeepSeek、GPT系列、本地Ollama），一键填充配置，降低使用门槛；

- 配置校验功能：保存配置时自动测试接口连通性，提示成功/失败原因；

- 多套配置保存：支持保存多组模型配置，一键快速切换工作模型；

- 拓展配置项：支持划词翻译功能独立开关配置，状态持久化，灵活管控功能启停。

### 3\.3 AI Agent网页智能理解与阅读辅助（用户核心诉求1）

#### 3\.3\.1 核心功能

AI Agent自动识别、清洗、解析当前网页内容，**针对性优化技术文档场景**，过滤广告、导航栏、侧边栏、推荐内容等冗余信息，只提取正文、代码块、参数说明、教程步骤等有效内容，作为AI对话的上下文依据。

#### 3\.3\.2 技术文档专属辅助能力（重点补充功能）

- 代码块智能识别：自动区分网页中的代码片段、注释、参数说明，支持代码解读、纠错、举例改写；

- 专业术语解析：自动识别技术名词、框架API、语法规则，对话中可一键解释术语；

- 长文档分段总结：针对超长技术手册，智能分段梳理核心知识点、使用场景、注意事项；

- 英文文档辅助翻译：结合划词翻译能力，全方位赋能英文技术文档阅读，精准解析专业术语、语句语境；

- 问题定位能力：用户提问后，Agent自动关联网页对应段落，精准解答，不脱离当前页面内容。

- 问题定位能力：用户提问后，Agent自动关联网页对应段落，精准解答，不脱离当前页面内容。

#### 3\.3\.3 通用网页能力

支持新闻、博客、教程、科普、文档等所有网页类型，自动适配解析规则，保证通用场景可用性。

### 3\.4 右侧悬浮对话小窗（用户核心诉求4）

#### 3\.4\.1 功能描述

插件激活后，可唤起**浏览器右侧悬浮独立小窗**，置顶于当前网页上层，用户可随时和AI Agent对话，提问网页疑惑、讨论技术知识点、解读页面内容。

#### 3\.4\.2 交互规则

- 唤起方式：插件图标点击「打开对话窗」按钮、快捷键唤起（可自定义）；

- 展示位置：浏览器页面右侧悬浮，默认宽度380px、高度自适应，不遮挡网页核心内容；

- 窗口特性：支持拖拽移动、拉伸调整尺寸、收起悬浮图标、全屏展开，跟随当前标签页切换；

- 对话逻辑：**所有对话基于当前网页上下文**，切换标签页后，Agent自动更新页面内容，对话上下文同步刷新。

#### 3\.4\.3 补充实用功能

- 快捷指令栏：内置「总结全文」「解读核心知识点」「翻译全文」「代码解析」「疑难答疑」一键快捷提问，适配技术文档高频需求；

- 对话记录本地保存：按网页域名分类保存对话记录，支持查看历史对话、清空记录；

- 流式输出回答：AI回答实时逐字展示，无需等待加载完成，体验流畅；

- 复制回答功能：一键复制AI输出的代码、解析内容、知识点总结；

- 深色/浅色模式切换：适配不同使用场景，降低长时间阅读视觉疲劳。

### 3\.5 对话总结导出功能（新增）

#### 3\.5\.1 功能描述

针对用户与AI Agent的全部网页咨询、知识点讨论、问题答疑对话内容，提供智能总结\+Markdown格式导出下载能力。功能采用**可插拔、多适配架构**，不仅满足当前本地MD文档下载需求，还预留标准化拓展接口，支持后续迭代新增服务器云端存储、浏览器控制台打印、多格式文件导出、数据同步等多种文档处理能力，无需重构核心代码。

#### 3\.5\.2 核心功能规则

- 总结范围：默认抓取当前网页会话下，用户与Agent的**全部历史对话内容**，包含提问内容、AI解答、代码解析、知识点讨论、疑难答疑等全部交互记录；

- 智能总结逻辑：支持两种模式切换，一是**完整对话留存模式**，原样整理所有对话为MD文档；二是**AI精简总结模式**，由大模型提炼对话核心问题、关键结论、技术知识点、解决方案，生成结构化精简文档；

- 文档规范：导出MD文档自带标准化格式，包含网页来源链接、对话时间、核心主题、问答分区、代码块高亮、知识点总结分区，适配技术文档阅读归档场景；

- 本地下载能力：一键触发下载，自动命名（网页标题\+时间戳），直接保存至本地设备，无格式错乱、无内容缺失。

#### 3\.5\.3 可插拔架构设计（预留未来拓展）

本功能统一封装文档处理核心底层，通过模块化插拔接口适配不同输出渠道，核心架构分层解耦，具体预留能力如下：

- 已落地能力：本地Markdown文档下载输出；

- 预留服务器存储拓展：预留云端API对接接口、数据加密上传模块、会话归档存储字段，后续可直接对接后台服务器，实现对话文档云端存储、在线查看、历史归档管理；

- 预留控制台打印拓展：预留浏览器Console日志输出接口，后续可一键将总结文档打印至浏览器控制台，支持开发者调试、日志导出；

- 预留多格式拓展：底层适配多文件格式转换，后续可快速新增TXT、PDF、HTML等格式导出能力；

- 拓展无侵入：所有新增输出渠道均为可插拔配置，开启/关闭不影响现有本地下载核心功能，兼容存量使用逻辑。

### 3\.6 划词翻译功能（可配置开关）

#### 3\.6\.1 功能描述

插件新增独立可管控划词翻译能力，用户可在插件配置面板自主开启或关闭该功能，支持全局生效/全局禁用。功能聚焦技术文档阅读场景，仅针对网页英文内容进行翻译，选中文字后自动唤起轻量化悬浮弹窗，同步展示**直译结果**与**网页上下文智能翻译结果**，选中为单个英文单词时，额外展示美式音标、英式音标，满足精准阅读、术语校对、英文文档解读需求。功能关闭后，无任何划词监听、无弹窗触发、无资源占用，完全静默。

#### 3\.6\.2 开关配置规则

- 配置入口：插件主配置面板新增「划词翻译功能」独立开关，默认关闭，状态本地持久化保存；

- 开启状态：全局网页生效，监听用户鼠标划选英文内容，自动触发翻译弹窗与翻译解析；

- 关闭状态：关闭划词监听逻辑，彻底禁用所有划词翻译相关能力，不干扰网页原生选中文本功能；

- 网页单独适配：继承插件网页单独禁用能力，单页禁用插件后，划词翻译同步失效。

#### 3\.6\.3 核心翻译展示规则

所有翻译能力依托当前网页上下文与已配置AI大模型生成，保证翻译准确性，区分单词、短句、长文本三种场景：

- 单个英文单词：弹窗展示**英式音标、美式音标** \+ 标准直译释义（多义项罗列） \+ 当前网页场景上下文适配翻译；

- 英文短句/短语：隐藏音标，展示精准字面直译 \+ 贴合网页技术场景的上下文意译；

- 长段英文文本：精简直译核心含义 \+ 结合整段网页内容的通顺上下文翻译，剔除冗余语义。

#### 3\.6\.4 悬浮弹窗交互规则

- 触发方式：用户鼠标拖动选中网页任意英文内容，松开鼠标后立即触发弹窗；

- 弹窗位置：跟随选中文本位置悬浮展示，自动避让浏览器边界，避免弹窗超出屏幕、遮挡正文内容；

- 弹窗样式：轻量化极简弹窗，适配插件深浅色模式，不破坏原生网页样式；

- 消失规则：点击网页空白处、重新选中文本、滚动页面时，弹窗自动消失；

- 无干扰规则：仅识别英文内容，选中中文、数字、符号时不触发弹窗，无无效触发。

#### 3\.6\.5 技术适配逻辑

划词翻译复用用户已配置的大模型接口，无需额外配置，统一接入当前生效模型，保证上下文翻译与网页AI解析逻辑一致、风格统一；翻译请求轻量化处理，响应速度快，适配技术文档高频划词场景，不产生多余网络请求与内存占用；翻译弹窗严格遵循全局现代简约UI规范，轻量化极简样式，适配Vue3主题模式。

### 3\.7 全局UI设计规范（现代简约风 · Vue3实现）

#### 3\.7\.1 整体设计风格定位

插件所有页面、弹窗、组件统一采用**现代简约风**设计，主打极简留白、低饱和配色、轻量化圆角、清晰层级、无冗余装饰，贴合技术工具类产品调性，降低长时间使用视觉疲劳，适配开发者、技术学习用户使用场景。所有UI组件基于Vue3生态现代UI框架开发，组件化、标准化、可复用，全局样式统一。

#### 3\.7\.2 技术实现要求

- 核心技术：Vue3 组合式API开发，摒弃老旧语法，适配插件轻量化打包；

- UI框架选型：支持Element Plus、Naive UI、Arco Design Vue任意主流现代Vue3 UI框架，优先轻量化、按需引入，减少插件体积；

- 样式方案：统一使用CSS变量管理主题，支持深浅色模式一键切换，全局样式隔离，不污染宿主网页样式；

- 组件规范：所有按钮、输入框、开关、弹窗、卡片、下拉框统一框架原生组件，保证交互一致性。

#### 3\.7\.3 全局视觉规范（强制统一）

- 配色体系：主色采用低饱和科技蓝，辅助色为浅灰、淡绿（成功）、浅红（报错），无高饱和刺眼配色，文字采用深灰/纯黑双层层级，保证可读性；

- 圆角规范：统一中度圆角，按钮、卡片、弹窗、输入框圆角尺寸统一，柔和简约；

- 留白规范"\>页面、卡片、组件统一留白间距，层级清晰，杜绝拥挤布局；

- 阴影规范：轻量化浅阴影，仅用于悬浮弹窗、卡片层级区分，无厚重黑阴影；

- 字体规范：跟随系统无衬线字体，字号层级分明，标题加粗、正文常规、辅助文字浅灰缩小。

#### 3\.7\.4 各功能模块UI适配要求

- 插件Popup配置面板：极简卡片式布局，功能分区清晰，开关、输入框、按钮整齐对齐，无多余装饰，参数配置列表规整清爽；

- 右侧悬浮对话窗：简约无边框/弱边框设计，聊天气泡区分用户与AI，代码块高亮简约美观，快捷指令栏轻量化平铺布局；

- 划词翻译悬浮小窗：极致轻量化极简弹窗，紧凑布局，音标、直译、上下文翻译分区清晰，体积小巧不遮挡内容；

- 功能按钮：统一简约扁平化按钮，无渐变、无复杂动效，hover浅色高亮反馈，点击有轻微缩放交互；

- 弹窗提示：成功、报错、提示弹窗统一简约风格，文字清晰、图标轻量化。

#### 3\.7\.5 交互动效规范

全程采用**轻量微动效**，拒绝花哨动画：弹窗平滑淡入淡出、窗口拖拽流畅、按钮hover静默反馈、切换页面无闪烁，保证工具类插件的高效、安静、专业的使用体验。

### 3\.8 补充通用优化功能（产品体验增强）

- 权限最小化：插件仅读取当前活跃标签页内容，无多余权限，保障用户隐私；

- 离线兼容：未配置模型/无网络时，提示用户配置接口，不报错崩溃；

- 资源轻量化：闲置时自动暂停网页解析，降低浏览器内存占用；

- 快捷键自定义：支持自定义开关插件、唤起对话窗、总结页面的快捷键；

- 隐私保护：所有配置、对话数据仅本地存储，不上传任何用户数据至第三方服务器；

- 划词翻译功能（可配置开关）：支持用户自主开启/关闭网页划词翻译能力，开启后选中网页英文内容即可唤起悬浮翻译弹窗，提供直译\+上下文智能翻译双模式，单个英文单词额外展示英美音标，适配技术文档英文阅读场景。

## 4\. 产品交互流程

### 4\.1 首次使用流程

1. 用户安装插件，默认开启全局开关；

2. 点击插件图标，进入配置页面，选择预设模型或自定义填写大模型接口信息；

3. 点击校验接口，配置成功后保存；

4. 点击「打开对话窗」，右侧悬浮窗口唤起，自动解析当前网页；

5. 用户开始提问、解读、讨论网页内容。

### 4\.2 日常使用流程

1. 打开任意网页（优先技术文档）；

2. 唤起右侧对话小窗，Agent自动更新页面上下文；

3. 手动提问或使用快捷指令获取解析、答疑内容；

4. 使用完成后收起或关闭窗口，插件静默待机。

### 4\.3 关闭插件流程

关闭全局开关后，立即停止网页监听、终止AI请求、清空临时上下文，插件进入完全静默状态。

## 5\. 技术规格与兼容要求

### 5\.1 浏览器兼容

支持Chrome 90\+、Edge内核浏览器、所有Chromium内核浏览器，兼容Windows、Mac、Linux系统。

### 5\.2 技术架构

- 采用Chrome Extension V3最新版本规范（主流商店通用、安全性更高）；

- Content Script实现网页内容抓取与解析；

- Popup面板实现开关、模型配置；

- Sidebar悬浮窗独立渲染，不污染原生网页DOM；

- LocalStorage本地加密存储配置与对话记录，文档导出模块采用可插拔架构，支持多渠道输出拓展。

- UI技术栈：整体采用**Vue3 \+ 现代简约UI框架**开发，轻量化组件化实现，适配Chrome插件环境，无冗余依赖，界面统一、简洁、轻量化。

### 5\.3 接口适配规范

统一兼容OpenAI API标准接口，支持所有主流国内外大模型，支持本地私有化部署模型对接。

## 6\. 非功能需求

### 6\.1 性能需求

- 网页解析耗时≤300ms，页面切换无明显延迟；

- 插件闲置内存占用≤20MB，不影响浏览器流畅度；

- AI请求超时可自定义，默认15s超时重试。

### 6\.2 隐私安全需求

- 用户密钥、对话记录、网页数据仅本地存储，无云端上传；

- 无埋点、无广告、无用户数据收集；

- 严格遵循Chrome插件安全规范，无高危权限。

### 6\.3 可用性需求

配置永久保存，重启浏览器、重启电脑不丢失；异常崩溃后自动恢复默认可用状态。

## 7\. 版本迭代预留功能（后续可扩展）

- 网页内容划词提问：选中页面任意文字，快速唤起AI答疑；

- 文档多格式导出：基于现有可插拔架构，拓展PDF、HTML、Word等格式导出能力；

- 自定义Agent人设：可设置技术专家、新手科普、代码优化师等不同Agent角色；

- 批量文档解析：支持多页面技术文档连续解读；

- 云端归档存储：基于可插拔接口对接服务器，实现对话MD文档云端存储、在线检索、历史归档；

- 控制台日志输出：开启开发者模式后，支持总结文档一键打印至浏览器控制台，适配调试场景。

## 8\. 验收标准

1. 全局开关可正常启停，开启生效、关闭完全静默，无后台占用；

2. 大模型可自定义配置、校验、多组切换，接口请求正常；

3. 右侧悬浮对话窗可正常唤起、拖拽、缩放、收起，适配所有网页；

4. AI Agent可精准解析网页，技术文档代码、术语、知识点解读准确；

5. 所有补充功能正常生效，包含对话MD总结下载、可插拔拓展能力、可配置划词翻译功能，插件运行流畅，无卡顿、无报错、无隐私风险；

6. 全页面UI符合现代简约风规范，Vue3组件化实现，界面统一、层级清晰、交互流畅，深浅色模式适配正常。

> （注：部分内容可能由 AI 生成）
